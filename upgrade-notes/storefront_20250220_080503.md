#### Add gtm payment event & fix payment_change event ([#3814](https://github.com/shopsys/shopsys/pull/3814))

- `payment` event is fired for each payment attempt
- `payment_change` event is now also fired from the `order-payment-confirmation` or `order-detail` pages
    - it is fired from `changePaymentInOrderHandler` after successful payment change
- see #project-base-diff to update your project
