{% block group_row %}
    <div class="card mb-5">
        <div class="card-header">
            <h3 class="card-title">{{ form.vars.label }}</h3>
        </div>
        <div class="card-body">
            {{ form_widget(form) }}
        </div>
    </div>
{% endblock %}
